import 'package:flutter/material.dart';

class SingleBoomBoxScreen extends StatelessWidget {
  const SingleBoomBoxScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
